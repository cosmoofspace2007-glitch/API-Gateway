server:
  port: 8080

spring:
  cloud:
    gateway:
      routes:
        - id: course-service
          uri: http://localhost:8081
          predicates:
            - Path=/api/courses/**

app:
  jwt:
    secret: mysecretkeymysecretkeymysecretkey123456
